nextflow.enable.dsl=2

workflow {

    /*
    goals:
        1. import the process GET_HEADERS from fasta_utils.nf in the
           modules directory
        2. run the pipeline using the provided fasta_seqs directory as
           input into GET_HEADERS
    */
}
